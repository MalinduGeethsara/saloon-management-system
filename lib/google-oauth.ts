// Shared between the Google OAuth start route and its callback (route files can't export extras).
export const GOOGLE_OAUTH_NONCE_COOKIE = 'google_oauth_nonce';
export const GOOGLE_OAUTH_NONCE_TTL_SECONDS = 10 * 60;
