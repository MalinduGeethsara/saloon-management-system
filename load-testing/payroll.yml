config:
  target: "http://localhost:3001"
  processor: "./processor.js"
  phases:
    - duration: 5
      arrivalCount: 3
      name: "3 owners viewing payroll"
  payload:
    path: "./staff-owner.csv"
    fields:
      - "email"
      - "password"

scenarios:
  - name: "Payroll page load"
    flow:
      - post:
          url: "/api/auth/staff-login"
          json:
            email: "{{ email }}"
            password: "{{ password }}"
          afterResponse: "captureCookie"
      - loop:
          - get:
              url: "/api/loadtest/payroll"
              headers:
                Cookie: "{{ authCookie }}"
          - think: 5
        count: 6
